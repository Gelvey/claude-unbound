__pycache__
.claude
.cursor
.pytest_cache
.ruff_cache
.serena
.venv
agent_workspace
.env
/logs/
server.log
/server.*.log
debug-*.log
.coverage
llama_cache
.smoke-results
.vscode

# MCP router — real config holds secrets; only the example template is tracked
scripts/mcp/mcp_config.json
scripts/mcp/.venv/
scripts/mcp/__pycache__/
scripts/mcp/*.pyc

# Managed FCC config directory (never part of the repo)
.fcc/

# Admin UI — built artefacts are committed to api/admin_static/; the source
# tree and node_modules stay out. End users never need Node.
node_modules/
frontend/dist/
*.tsbuildinfo
# Graphify extraction output — generated per-index run, not source.
graphify-out/

# Freebuff2API — a separate repo (github.com/Gelvey/Freebuff2API), tracked
# locally as its own checkout. It was previously committed as an undeclared
# gitlink; keep it out of this repo.
Freebuff2API/
