## Summary

<!-- Brief description of the change and why -->

## Frontend (admin UI)

- [ ] N/A — this PR does not touch `frontend/` or the admin UI.
- [ ] I ran `npm run build` and committed the regenerated `api/admin_static/` artefacts in this change (the `admin-build` CI job rejects stale artefacts; do not hand-edit `admin.js`/`admin.css`/`index.html`).
- [ ] I added/updated Vitest coverage under `frontend/src/**/__tests__/` for new component/helper behavior and `npm test` is green.

## Tests

- [ ] `./scripts/ci.sh` passes locally (or I called out which checks I ran).

## Risk

<!-- Any residual risk, migration steps, or follow-ups -->
